let viewer3D;let selectedProduct=null;const selectTipoProducto=document.getElementById('tipo-produto');const selectMarca=document.getElementById('marca');const selectModelo=document.getElementById('modelo');const specsList=document.getElementById('specs-list');const btnReset=document.getElementById('btn-reset');const loading=document.getElementById('loading');document.addEventListener('DOMContentLoaded',()=>{viewer3D=new Viewer3D('canvas-container');setupEventListeners()});function setupEventListeners(){selectTipoProducto.addEventListener('change',onTipoProductoChange);selectMarca.addEventListener('change',onMarcaChange);selectModelo.addEventListener('change',onModeloChange);btnReset.addEventListener('click',onResetClick)}function onTipoProductoChange(){const tipo=selectTipoProducto.value;selectMarca.innerHTML='<option value="">Selecione uma marca...</option>';selectModelo.innerHTML='<option value="">Selecione um modelo...</option>';specsList.innerHTML='<p class="empty-specs">Selecione um produto para ver as especificações</p>';if(!tipo)return;const marcas=Object.keys(PRODUTOS[tipo==='disjuntor'?'disjuntores':'quadros']);marcas.forEach(marca=>{const option=document.createElement('option');option.value=marca;option.textContent=marca;selectMarca.appendChild(option)})}function onMarcaChange(){const tipo=selectTipoProducto.value;const marca=selectMarca.value;selectModelo.innerHTML='<option value="">Selecione um modelo...</option>';specsList.innerHTML='<p class="empty-specs">Selecione um produto para ver as especificações</p>';if(!tipo||!marca)return;const tipoKey=tipo==='disjuntor'?'disjuntores':'quadros';const modelos=PRODUTOS[tipoKey][marca];modelos.forEach(modelo=>{const option=document.createElement('option');option.value=modelo.id;option.textContent=modelo.modelo;selectModelo.appendChild(option)})}function onModeloChange(){const tipo=selectTipoProducto.value;const marca=selectMarca.value;const modeloId=selectModelo.value;if(!tipo||!marca||!modeloId)return;const tipoKey=tipo==='disjuntor'?'disjuntores':'quadros';const modelos=PRODUTOS[tipoKey][marca];const produto=modelos.find(m=>m.id===modeloId);if(!produto)return;selectedProduct=produto;showSpecs(produto);showLoading(true);setTimeout(()=>{viewer3D.createProductGeometry(produto);showLoading(false)},300)}function showSpecs(produto){specsList.innerHTML='';Object.entries(produto.specs).forEach(([label,value])=>{const specItem=document.createElement('div');specItem.className='spec-item';specItem.innerHTML=`<span class="spec-label">${label}:</span><span class="spec-value">${value}</span>`;specsList.appendChild(specItem)})}function onResetClick(){viewer3D.reset()}function showLoading(show){loading.classList.toggle('active',show)}console.log('QDC Interativo - Aplicação carregada!');console.log('Produtos disponíveis:',PRODUTOS);